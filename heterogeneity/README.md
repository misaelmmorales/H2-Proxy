# Multi-scale spatiotemporal proxy for $H_2$ storage prediction

Vision transformer with residual concatenations to predict $H_2$ pressure and saturation plume migration during underground gas storage.
